mutation InsertWishList($title: String!, $description: String!) {
    insert_familycloud_wish_list_one(object: {title: $title, description: $description}) {
        id
        description
        title
    }
}
