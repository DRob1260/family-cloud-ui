mutation insertWishListItem($wishListId: Int!, $title: String!, $description: String, $url: String) {
    insert_familycloud_wish_list_item(objects: {wish_list_id: $wishListId, title: $title, description: $description, url: $url}) {
        returning {
            id
        }
    }
}
